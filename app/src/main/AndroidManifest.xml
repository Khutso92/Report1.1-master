<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mlab_codetribe.report11">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminActivity"
            android:label="Admin"/>
        <activity android:name=".AddTeacherStudentActivity"
            android:label="Add Teacher/Student "/>
        <activity android:name=".TeacherActivity"
            android:label="Teacher"/>
        <activity android:name=".AddSubjectMark"
            android:label="Add subject mark"/>
        <activity android:name=".UpdateSubjectMarkActivity"
            android:label="Update subject mark"/>
        <activity android:name=".StudentActivity"
            android:label="Student report"></activity>
    </application>

</manifest>